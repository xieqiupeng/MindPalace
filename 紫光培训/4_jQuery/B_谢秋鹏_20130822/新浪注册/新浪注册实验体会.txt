首先是关于图片的问题：

需要点击的图片使用img标签，用来触发js或者jquery动画效果。

不需要的一律设置为div背景图片，方便布局，无法选中的话也美观。

table的padding不会影响width。

定位有3种，position的级别最高，然后是margin，最后是float

column：列，row 行

javascript用来做验证，而jquery用来做动画。

javascript用来定义function，而jquery用来触发事件。

触发效果一律使用jquery。不在html添加onclick等。

关于utf8与gb2312

http://kb.cnblogs.com/page/60552/

我如果要使用innerHTML插入变量怎么办？

jquery 传参    
$("#test").bind("click",{id:100,name:'Hello'},function(event){
alert(event.data.id);
alert(event.data.name);
});
使用bind方法，将一个匿名函数绑定到按钮的click事件，bind方法的第二个参数就是传递的参数，传递的参数在funtion中使用【event.data.参数名】的方式获取，这个【event】参数不要忘记写了哈

$(".button").click(function(){
    var name = $(this).attr("myvalue");
    alert(name);
});

传参传对象比变量好得多，反正还得变成变量。

td元素和td元素之间有一个元素就是text，就是你能在两个td间写字的那个元素。

submit
button
image的区别
submit刷新界面，
button与submit都有默认样式，image没有文字。